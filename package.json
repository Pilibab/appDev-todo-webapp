{
  "devDependencies": {
    "concurrently": "^9.2.1"
  },
  "scripts": {
    "dev": "concurrently \"cd backend && node src/index.js\" \"npm run dev --prefix frontend\"",
    "install-all": "cd frontend && npm install && cd ../backend && npm install"
  },
  "dependencies": {
    "cors": "^2.8.5"
  }
}
